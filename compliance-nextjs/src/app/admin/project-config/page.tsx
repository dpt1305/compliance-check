import ProjectConfig from '@/components/admin/ProjectConfig';

export const metadata = { title: 'Project Config | Compliance Admin' };

export default function ProjectConfigPage() {
  return <ProjectConfig />;
}
