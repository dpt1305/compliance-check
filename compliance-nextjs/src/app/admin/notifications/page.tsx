import Notifications from '@/components/admin/Notifications';

export const metadata = { title: 'Notifications | Compliance Admin' };

export default function NotificationsPage() {
  return <Notifications />;
}
